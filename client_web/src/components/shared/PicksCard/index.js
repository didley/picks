import ProfileHeader from "./PrifileHeader";
import PicksList from "./PicksList";

const PicksCard = () => {
  return (
    <div>
      PicksCard
      <ProfileHeader />
      <PicksList />
    </div>
  );
};

export default PicksCard;
