name: 'Test Report'
on:
  workflow_run:
    workflows: ['CI/*']                     # runs after CI workflow
    types:
      - completed
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
    - uses: dorny/test-reporter@v1.5.0
      with:
        artifact: test-output # artifact name
        name: JEST Tests                  # Name of the check run which will be created
        path: '**/test-output.json'                     # Path to test results (inside artifact .zip)